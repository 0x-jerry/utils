name: Test

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Setup bun
        uses: 0x-jerry/workflows/actions/setup-js@main

      - name: Install dependencies
        run: bun install

      - name: Test
        run: bun run test
